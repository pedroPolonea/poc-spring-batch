spring:
  datasource:
    url: jdbc:mysql://localhost:3315/sb-poc-11?serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
      show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        type: trace
  batch:
    initialize-schema: never